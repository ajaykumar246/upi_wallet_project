# Database Config
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=

# Redis Config
REDIS_PORT=

# IMPORTANT: 
# When running locally (node server.js), use 'localhost'
# When running in Docker, we will override this environment variable.
DB_HOST=
REDIS_HOST=


JWT_SECRET=